# QuantaRoute Demo Configuration
# Copy this file to config.env and update with your deployed API details

# =============================================================================
# API MODE CONFIGURATION
# =============================================================================

# Mode: 'local' or 'remote'
# - local: Uses localhost:8000 (runs demo backend locally)
# - remote: Uses deployed production API
QUANTAROUTE_DEMO_MODE=local

# =============================================================================
# REMOTE API CONFIGURATION (Only needed if mode=remote)
# =============================================================================

# Your deployed QuantaRoute API URL
# Example: https://your-app.up.railway.app
# Example: https://quantaroute-api.herokuapp.com
# Example: https://api.yourdomain.com
QUANTAROUTE_API_URL=https://your-app.up.railway.app/api/v1

# API Key for authentication (required for remote mode)
# Get this from your deployed API dashboard
# Demo key (replace with your production key):
QUANTAROUTE_API_KEY=PROD_API_KEY

# =============================================================================
# LOCAL MODE CONFIGURATION (Only needed if mode=local)
# =============================================================================

# Local backend port (default: 8000)
LOCAL_BACKEND_PORT=8000

# Local API base URL
LOCAL_API_URL=http://localhost:8000/api

# =============================================================================
# DATA CONFIGURATION
# =============================================================================

# Path to OSM PBF data file (relative to project root)
OSM_DATA_PATH=../test-data/bengaluru-highways.osm.pbf

# Profiles directory
PROFILES_DIR=../profiles

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================

# If deploying the demo app itself (not just using remote API)
DEMO_HOST=0.0.0.0
DEMO_PORT=8000

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# =============================================================================
# FEATURE FLAGS
# =============================================================================

# Enable/disable features
ENABLE_ELEVATION_PROFILE=true
ENABLE_TRAFFIC_INTEGRATION=false
ENABLE_REAL_TIME_UPDATES=false
ENABLE_EXPLORATION_VISUALIZER=true

# =============================================================================
# NOTES
# =============================================================================

# Local Mode:
#   - Runs demo backend on localhost
#   - Loads Bengaluru OSM data locally
#   - No API key needed
#   - Good for: Development, testing, offline demos
#
# Remote Mode:
#   - Connects to deployed production API
#   - Requires API key
#   - No local data loading
#   - Good for: Public demos, production use, sharing with users
#
# How to switch modes:
#   1. Update QUANTAROUTE_DEMO_MODE
#   2. For remote mode, set QUANTAROUTE_API_URL and QUANTAROUTE_API_KEY
#   3. Restart the demo
#
# Or switch in the browser:
#   - Click the mode indicator in the top-right corner
#   - Or add ?mode=local or ?mode=remote to URL

